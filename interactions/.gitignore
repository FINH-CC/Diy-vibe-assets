# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist-ssr
*.local

# dist/ is intentionally tracked: the dashboard one level up links directly
# to interactions/dist/ as static files, and nothing builds it automatically
# on deploy. Run `npm run build` here and commit the result after any change.

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
